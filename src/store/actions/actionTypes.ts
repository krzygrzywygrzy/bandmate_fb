export enum UserActionType {
  LOAD = "user_load",
  LOADED = "user_loaded",
  ERROR = "user_error",
}

export enum SwipesActionType {
  LOAD = "swipes_load",
  LOADED = "swipes_loaded",
  ERROR = "swipes_error",
}