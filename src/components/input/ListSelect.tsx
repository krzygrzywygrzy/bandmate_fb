import React from "react";

type Props = {
  data: string[];
  setData(data: string[]): void;
};

const ListSelect: React.FC<Props> = ({ data, setData }) => {
  return <div></div>;
};

export default ListSelect;
